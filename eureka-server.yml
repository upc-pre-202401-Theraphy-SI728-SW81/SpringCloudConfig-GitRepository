server:
  port: 8761

eureka:
  instance:
    hostname: localhost1
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/,http://${eureka.instance.hostname}:8762/eureka/
  server:
    waitTimeInMsWhenSyncEmpty: 0
    response-cache-update-interval-ms: 5000
    eviction-interval-timer-in-ms: 10000 # Ajusta según tus necesidades

---

server:
  port: 8762

eureka:
  instance:
    hostname: localhost2
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/,http://${eureka.instance.hostname}:8762/eureka/
  server:
    waitTimeInMsWhenSyncEmpty: 0
    response-cache-update-interval-ms: 5000
    eviction-interval-timer-in-ms: 10000 # Ajusta según tus necesidades
