server:
  port: 8080 #the default port used for zuul service
  
zuul:
  ignoredServices: '*'
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 20000
    routes:
      echo-service:
        path: /echo/** #path that will be intercepted
        uri: ${ECHO_ROUTE_URI:http://laptop-rj9q8eje:8091}
